# Extension Roadmap

- Multi-strategy
  - add strategy registry, per-strategy budgets, and isolated performance attribution
- Multi-account
  - isolate risk state and confirmation tokens per account
- Multi-exchange
  - extend adapter registry and reconciliation workers per venue
- Backtest integration
  - reuse `ExecutionMode.BACKTEST` with deterministic market data playback
- Model evaluation
  - log validated AI decisions, realized outcomes, reject rates, and per-strategy calibration
